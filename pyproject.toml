[project]
name = "taskclimanager"
version = "0.1.0"
description = "Task CLI DevOps project for managing tasks from the command line."
requires-python = ">=3.10"

[project.scripts]
tareas = "taskcli.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["taskcli"]